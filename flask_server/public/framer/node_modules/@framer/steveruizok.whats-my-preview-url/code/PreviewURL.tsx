import * as React from "react"
import { Frame } from "framer"
import { url } from "framer/resource"

export function PreviewURL() {
    return (
        <Frame
            size={"100%"}
            background="#FFF"
            style={{
                height: "100%",
                width: "100%",
                display: "flex",
                flexDirection: "column",
                fontSize: 24,
                fontWeight: 600,
                color: "#333",
                placeContent: "center",
                placeItems: "center",
                textAlign: "center",
            }}
        >
            <div>Your local preview URL is</div>
            <div>
                <a target="_blank" href={url()}>
                    {url()}
                </a>
            </div>
        </Frame>
    )
}

PreviewURL.defaultProps = {
    height: 128,
    width: 320,
}
